# Option File Editor Pro

Tela inicial para carregar qualquer arquivo, sem restringir por extensao.

## Como executar

Abra o arquivo `index.html` no navegador.

Tambem existe uma versao em Python:

```powershell
python app.py
```

Depois clique em **Carregar arquivo** e selecione o arquivo desejado.
